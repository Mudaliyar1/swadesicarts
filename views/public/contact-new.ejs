<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style-new.css">
</head>
<body>
    <%- include('../partials/navbar-new') %>
    
    <!-- PAGE HEADER -->
    <section class="page-header">
        <div class="container">
            <h1>Contact Us</h1>
            <p><%= siteSettings && siteSettings.contact ? siteSettings.contact.heading : "We'd love to hear from you. Get in touch with us today!" %></p>
        </div>
    </section>

    <!-- CONTACT SECTION -->
    <section>
        <div class="container">
            <% if (locals.messages && messages.success) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="bi bi-check-circle"></i> <%= messages.success %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>
            <% if (locals.messages && messages.error) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-circle"></i> <%= messages.error %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>

            <div class="row g-5">
                <!-- CONTACT INFO -->
                <div class="col-lg-4">
                    <h2 class="mb-4"><%= siteSettings && siteSettings.contact ? siteSettings.contact.subheading : 'Get In Touch' %></h2>
                    
                    <div class="mb-4">
                        <div class="d-flex align-items-start gap-3 mb-4">
                            <div class="feature-icon" style="flex-shrink: 0;">
                                <i class="bi bi-map"></i>
                            </div>
                            <div>
                                <h5 class="mb-2">Location</h5>
                                <p class="text-muted-custom mb-0"><%= siteSettings && siteSettings.contact ? siteSettings.contact.location : 'India' %></p>
                            </div>
                        </div>

                        <div class="d-flex align-items-start gap-3 mb-4">
                            <div class="feature-icon" style="flex-shrink: 0;">
                                <i class="bi bi-envelope"></i>
                            </div>
                            <div>
                                <h5 class="mb-2">Email</h5>
                                <a href="mailto:<%= siteSettings && siteSettings.contact ? siteSettings.contact.email : 'info@swadesicarts.com' %>" class="text-accent text-decoration-none">
                                    <%= siteSettings && siteSettings.contact ? siteSettings.contact.email : 'info@swadesicarts.com' %>
                                </a>
                            </div>
                        </div>

                        <div class="d-flex align-items-start gap-3 mb-4">
                            <div class="feature-icon" style="flex-shrink: 0;">
                                <i class="bi bi-telephone"></i>
                            </div>
                            <div>
                                <h5 class="mb-2">Phone</h5>
                                <a href="tel:<%= siteSettings && siteSettings.contact ? siteSettings.contact.phone : '+91-000-0000000' %>" class="text-accent text-decoration-none">
                                    <%= siteSettings && siteSettings.contact ? siteSettings.contact.phone : '+91-000-0000000' %>
                                </a>
                            </div>
                        </div>

                        <div class="d-flex align-items-start gap-3">
                            <div class="feature-icon" style="flex-shrink: 0;">
                                <i class="bi bi-clock"></i>
                            </div>
                            <div>
                                <h5 class="mb-2">Business Hours</h5>
                                <p class="text-muted-custom mb-0">
                                    <%= siteSettings && siteSettings.contact ? siteSettings.contact.businessHours.days : 'Monday - Saturday' %><br>
                                    <%= siteSettings && siteSettings.contact ? siteSettings.contact.businessHours.time : '9:00 AM - 6:00 PM IST' %>
                                </p>
                            </div>
                        </div>
                    </div>

                    <% if (siteSettings && siteSettings.socialMedia && (siteSettings.socialMedia.facebook || siteSettings.socialMedia.twitter || siteSettings.socialMedia.instagram || siteSettings.socialMedia.linkedin || siteSettings.socialMedia.youtube)) { %>
                        <div class="mt-5 pt-5 border-top">
                            <h5 class="mb-3">Follow Us</h5>
                            <div class="d-flex gap-2">
                                <% if (siteSettings.socialMedia.facebook) { %>
                                    <a href="<%= siteSettings.socialMedia.facebook %>" target="_blank" class="btn btn-sm btn-outline-primary rounded-circle p-2">
                                        <i class="bi bi-facebook"></i>
                                    </a>
                                <% } %>
                                <% if (siteSettings.socialMedia.twitter) { %>
                                    <a href="<%= siteSettings.socialMedia.twitter %>" target="_blank" class="btn btn-sm btn-outline-primary rounded-circle p-2">
                                        <i class="bi bi-twitter"></i>
                                    </a>
                                <% } %>
                                <% if (siteSettings.socialMedia.instagram) { %>
                                    <a href="<%= siteSettings.socialMedia.instagram %>" target="_blank" class="btn btn-sm btn-outline-primary rounded-circle p-2">
                                        <i class="bi bi-instagram"></i>
                                    </a>
                                <% } %>
                                <% if (siteSettings.socialMedia.linkedin) { %>
                                    <a href="<%= siteSettings.socialMedia.linkedin %>" target="_blank" class="btn btn-sm btn-outline-primary rounded-circle p-2">
                                        <i class="bi bi-linkedin"></i>
                                    </a>
                                <% } %>
                                <% if (siteSettings.socialMedia.youtube) { %>
                                    <a href="<%= siteSettings.socialMedia.youtube %>" target="_blank" class="btn btn-sm btn-outline-primary rounded-circle p-2">
                                        <i class="bi bi-youtube"></i>
                                    </a>
                                <% } %>
                            </div>
                        </div>
                    <% } %>
                </div>

                <!-- CONTACT FORM -->
                <div class="col-lg-8">
                    <div class="card border-0 shadow-sm">
                        <div class="card-body p-5">
                            <h3 class="mb-4">Send Us a Message</h3>
                            <form method="POST" action="/contact">
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Full Name *</label>
                                        <input type="text" class="form-control" name="name" required>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Mobile Number *</label>
                                        <input type="tel" class="form-control" name="mobile" required>
                                    </div>
                                    <div class="col-12 mb-3">
                                        <label class="form-label">Email Address *</label>
                                        <input type="email" class="form-control" name="email" required>
                                    </div>
                                    <div class="col-12 mb-3">
                                        <label class="form-label">Your Message *</label>
                                        <textarea class="form-control" name="requirement" rows="5" required></textarea>
                                    </div>
                                    <div class="col-12">
                                        <button type="submit" class="btn btn-accent btn-lg w-100">
                                            <i class="bi bi-send"></i> Send Message
                                        </button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <%- include('../partials/footer-new') %>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
